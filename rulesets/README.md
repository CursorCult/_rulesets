# Rulesets

Each file `rulesets/<NAME>.txt` defines a named ruleset as newline-separated rule names.

Rules are CursorCult rule pack repos (repos that do **not** start with `.` or `_`).

Only rules with a `v1` tag are allowed.

